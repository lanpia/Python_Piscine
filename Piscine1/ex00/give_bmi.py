import numpy as np

def give_bmi(height: list[int | float], weight: list[int | float]) -> list[int | float]:
    """주어진 신장(height)과 체중(weight)을 통해 각각의 BMI를 계산하여 numpy 배열로 반환합니다."""
	


def apply_limit(bmi: list[int | float], limit: int) -> list[bool]:
    """BMI numpy 배열과 제한값(limit)을 받아 제한을 초과하는 경우 True, 그렇지 않으면 False를 반환하는 numpy 배열을 생성합니다."""
